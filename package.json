{
  "name": "nestbud",
  "version": "1.0.0",
  "description": "NestBud — Find your bud, find your nest.",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"cd backend && npm start\" \"cd frontend && npm run dev\"",
    "vercel-build": "npm install --prefix backend && npm install --prefix frontend && npm --prefix frontend run build",
    "build": "cd frontend && npm run build",
    "start": "cd backend && node server.js",
    "seed": "cd backend && node seed.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
